<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
			
<mapper namespace="GroupMapper">

  	<resultMap id="groupSelectMap" type="group">		
		<result property="groupName" column="group_name" jdbcType="VARCHAR"/>
		<result property="userNo" column="user_no" jdbcType="INTEGER" />
	</resultMap>  	
  	
  	
 
  	
  	
  	<insert id="addGroup"	parameterType="group">
  		INSERT	
  			INTO tb_group(group_no, user_no, group_name, group_date)
  			VALUES(seq_tb_group_group_no.nextval, #{userNo}, #{groupName}, SYSDATE)				
  	</insert> 
  	
  	
  	
  		
  	
  	<!-- 
  	 <select id="getGroup" parameterType="int" resultMap="groupSelectMap">
		SELECT
			group_name		
			FROM tb_group 
				WHERE group_no = #{value}
	 </select>
	 
	 <sql id="getJoinQuery">
		SELECT
		t.group_name,

		u.id, u.pw,
		u.email, u.user_name,  u.tel,
	
		FROM tb_group t, tb_user u
	</sql>

 -->

  	
  	
</mapper>